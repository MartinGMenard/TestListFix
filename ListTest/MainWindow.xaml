<Window x:Class="ListTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ListTest"
        mc:Ignorable="d"
        Title="Orders" Height="520" Width="900"
        WindowStartupLocation="CenterScreen">


    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top toolbar -->
        <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,12">
            <TextBlock Text="Orders" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center"/>

            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="Refresh" Width="110" Margin="0,0,10,0" Click="Refresh_Click"/>
          
            </StackPanel>
        </DockPanel>

        <!-- Data grid + loading overlay -->
        <Border Grid.Row="1" BorderBrush="#DDD" BorderThickness="1" CornerRadius="6">
            <Grid>
                <DataGrid ItemsSource="{Binding Orders}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          RowHeight="30">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="80"/>
                        <DataGridTextColumn Header="Patient" Binding="{Binding Patient}" Width="*"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="160"/>
                        <DataGridTextColumn Header="Created" Binding="{Binding Created}" Width="200"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Loading overlay -->
                <Border Background="#80FFFFFF">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>

                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <ProgressBar Width="220" Height="12" IsIndeterminate="True"/>
                        <TextBlock Text="Loading..." Margin="0,10,0,0"
                                   HorizontalAlignment="Center" FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <TextBlock Grid.Row="2" Margin="0,10,0,0" Foreground="#666" TextWrapping="Wrap">
            Click Refresh, the loading bar doesn't work correctly and the data does not show up.
        </TextBlock>
    </Grid>
</Window>
